{
  "name": "form-submission-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone frontend that renders and submits the form via the Laravel API.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
